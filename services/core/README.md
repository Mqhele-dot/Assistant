# Core Service (FastAPI)

Run locally:
```bash
pip install -r requirements.txt
uvicorn main:app --reload
```

Endpoints:
- `POST /chat`
- `POST /codex/run`
- `GET /projects` / `POST /projects`
- `GET /logs`
- `POST /permissions`
